
const ErrorMessage = () => {
  return (<div data-testid='error-message' className='error'>Opps.. It looks like the movie you are looking for cannot be found.</div>);
};

export default ErrorMessage;
